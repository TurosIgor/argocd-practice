backups:
  pgbackrest:
    global:
      repo1-path: /pgbackrest/postgres-operator/postgres-pg-db/repo1
    repos:
      - name: repo1
        volume:
          volumeClaimSpec:
            accessModes:
              - ReadWriteOnce
            resources:
              requests:
                storage: 5Gi
pmm:
  enabled: true
  secret: postgres-pg-db-pmm-secret
  serverHost: https://monitoring-service.postgres.svc.cluster.local